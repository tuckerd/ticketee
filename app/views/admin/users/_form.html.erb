<% form_for [:admin, @user] do |f| %>
  The partial is rendering...something else is wrong...
  <%= f.error_messages %>
  <p>
    <%= f.label :email %>
    <%= f.text_field :email %>
  </p>
  <p>
    <%= f.label :password %>
    <%= f.text_field :password %>
  </p>
  <%= f.submit %>
<% end %>
